\begin{figure}[htbp]
  \centering
  \resizebox{0.96\textwidth}{!}{%
  \begin{tikzpicture}[
    font=\small,
    box/.style={draw=wmgdarkblue!55, rounded corners=2pt, align=center,
      text width=31mm, minimum height=12mm, fill=wmgblue!6},
    key/.style={box, fill=wmgblue!14, draw=wmgblue!75},
    score/.style={box, fill=green!8, draw=green!45!black},
    arrow/.style={-{Latex[length=2.2mm]}, thick, draw=wmgdarkblue!70}
  ]
    \node[box] (input) at (0,4.8) {Four 768-dimensional\\SigLIP item embeddings\\plus position mask};
    \node[key] (projection) at (5.0,4.8) {Layer normalisation,\\linear projection and GELU};
    \node[key] (slots) at (10.0,4.8) {Concatenate learned\\position embeddings};

    \node[box] (pooled) at (5.0,1.5) {Masked mean\\outfit summary};
    \node[box] (pairs) at (10.0,1.5) {Six absolute pairwise\\difference vectors\\and masked mean};
    \node[key] (concat) at (7.5,-1.8) {Concatenate global and\\pairwise summaries};
    \node[key] (mlp) at (12.5,-1.8) {Two-layer MLP\\with GELU and dropout};
    \node[score] (logit) at (17.5,-1.8) {Compatibility logit\\and calibrated score};

    \draw[arrow] (input) -- (projection);
    \draw[arrow] (projection) -- (slots);
    \draw[arrow] (slots) -- (pairs);
    \draw[arrow] (slots) -- (pooled);
    \draw[arrow] (pairs) -- (concat);
    \draw[arrow] (pooled) -- (concat);
    \draw[arrow] (concat) -- (mlp);
    \draw[arrow] (mlp) -- (logit);
  \end{tikzpicture}}
  \caption{Architecture of the task-specific lightweight compatibility model}
  \label{fig:model-architecture}
\end{figure}
